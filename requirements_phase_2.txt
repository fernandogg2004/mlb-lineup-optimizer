# =============================================================================
# MLB Lineup Optimizer — Phase 2 Dependencies
# Feature Engineering, Deep Learning & Probabilistic Modeling
# Python 3.11+  |  Extends requirements.txt (Phase 1 must be installed first)
# Last verified: 2026-05-18
# =============================================================================

# ---------------------------------------------------------------------------
# DataFrame / Array computation
# Polars: 10–100× faster than pandas for group-by + rolling window operations.
# Upgrade from Phase 1 pin to get improved rolling API.
# ---------------------------------------------------------------------------
polars==0.20.31
numpy==1.26.4          # stable; torch 2.x requires numpy <2.0

# ---------------------------------------------------------------------------
# Machine Learning
# ---------------------------------------------------------------------------
scikit-learn==1.8.0
scipy==1.17.1
statsmodels==0.14.2    # LOWESS, Weibull AFT, regression diagnostics

# ---------------------------------------------------------------------------
# Deep Learning (PyTorch ecosystem)
# CPU-only wheel — for GPU training use the SageMaker training job image.
# Replace the index URL with the CUDA 12.1 wheel for local GPU development.
# ---------------------------------------------------------------------------
torch==2.12.0
tensorboard==2.20.0
# Optional: PyTorch Lightning for structured training loops (used in 2.6)
lightning==2.2.4

# ---------------------------------------------------------------------------
# Probabilistic / Bayesian modeling
# ---------------------------------------------------------------------------
pymc==5.15.0            # Hierarchical Bayesian models (NUTS sampler via PyTensor)
arviz==0.18.0           # Posterior analysis & visualization
# Note: pymc installs pytensor automatically; no separate pin needed

# ---------------------------------------------------------------------------
# Clustering & Dimensionality Reduction (pitch profiling — 2.4)
# ---------------------------------------------------------------------------
hdbscan==0.8.43         # Density-based clustering, robust to noise
umap-learn==0.5.6       # 2D embedding visualization for player embeddings

# ---------------------------------------------------------------------------
# Survival analysis (bullpen starter duration — 2.5)
# ---------------------------------------------------------------------------
lifelines==0.29.0

# ---------------------------------------------------------------------------
# Gradient Boosting (park factor & bullpen models — 2.3, 2.5)
# ---------------------------------------------------------------------------
xgboost==2.0.3
catboost==1.2.5
shap==0.45.1            # SHAP values for model explainability

# ---------------------------------------------------------------------------
# Experiment Tracking
# ---------------------------------------------------------------------------
mlflow==2.13.2

# ---------------------------------------------------------------------------
# Visualization
# ---------------------------------------------------------------------------
matplotlib==3.10.9
seaborn==0.13.2

# ---------------------------------------------------------------------------
# Utilities
# ---------------------------------------------------------------------------
joblib==1.5.3           # Parallel model training
tqdm==4.67.3            # Progress bars in training loops
