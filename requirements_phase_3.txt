# =============================================================================
# MLB Lineup Optimizer — Phase 3 Dependencies
# Predictive Modeling, Monte Carlo Simulation & Combinatorial Optimization
# Python 3.11+  |  Extends requirements_phase_2.txt
# Last verified: 2026-05-18
#
# Compatibility notes:
#   numba 0.59+ requires numpy < 2.0 (already pinned in phase_1)
#   ray 2.10+ requires Python 3.8+; use ray[default] for the full runtime
#   DEAP 1.4.1 is pure-Python, no binary deps
# =============================================================================

# ---------------------------------------------------------------------------
# Gradient Boosting — primary PA-level model
# ---------------------------------------------------------------------------
lightgbm==4.3.0

# CatBoost already in requirements_phase_2.txt (1.2.5)
# XGBoost already in requirements_phase_2.txt (2.0.3)

# ---------------------------------------------------------------------------
# Numba — JIT compilation for inner simulation loop
# Requires LLVM; ships pre-compiled wheels on all major platforms.
# ---------------------------------------------------------------------------
numba==0.59.1

# ---------------------------------------------------------------------------
# Ray — distributed simulation parallelism
# ray[default] includes the dashboard, autoscaler, and remote-task runtime.
# For local-only development, ray[default] still works (single-node cluster).
# ---------------------------------------------------------------------------
ray[default]==2.20.0

# ---------------------------------------------------------------------------
# Evolutionary / Combinatorial Optimization
# ---------------------------------------------------------------------------
deap==1.4.1

# ---------------------------------------------------------------------------
# Hyperparameter Optimization (top-10 refinement layer 3)
# ---------------------------------------------------------------------------
optuna==3.6.0
optuna-integration[lightgbm]==3.6.0   # LightGBM-aware pruner

# ---------------------------------------------------------------------------
# Model calibration & evaluation (supplements scikit-learn from Phase 2)
# ---------------------------------------------------------------------------
# scikit-learn already in requirements_phase_2.txt (1.5.0)
# Explicitly pull reliability-diagram utilities
netcal==1.3.5       # Expected Calibration Error + reliability diagrams

# ---------------------------------------------------------------------------
# Plotting (calibration curves, reliability diagrams)
# ---------------------------------------------------------------------------
plotly==5.22.0

# ---------------------------------------------------------------------------
# Survival analysis (starter duration model)
# Already in Phase 2 via lifelines==0.29.0
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# OR-Tools (optional exact solver for constrained lineup problems)
# Skip if ortools installation fails — GA still works standalone
# ---------------------------------------------------------------------------
# ortools==9.9.3963   # uncomment if exact solver layer is needed

# ---------------------------------------------------------------------------
# Testing additions for Phase 3
# ---------------------------------------------------------------------------
hypothesis==6.100.1     # property-based testing for simulation invariants
