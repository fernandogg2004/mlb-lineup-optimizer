# =============================================================================
# MLB Lineup Optimizer — Phase 6 Dependencies
# UI Dashboard · What-If Engine · Game-Day Notifier
# Python 3.11+  |  Extends requirements_phase_5.txt
# Last verified: 2026-05-18
#
# Compatibility notes:
#   streamlit 1.36+ includes st.data_editor (required for inline lineup editing).
#   reportlab 4.x is the maintained fork of the original; API is stable since 3.x.
#   slack-sdk 3.30 supports files_upload_v2 (v1 deprecated March 2025).
#   kaleido 0.2.1 enables Plotly → PNG export for PDF embedding (headless).
#   jinja2 is typically a transitive dep of many packages; pin it explicitly
#   to avoid version conflicts with FastAPI / LlamaIndex internals.
# =============================================================================

# ---------------------------------------------------------------------------
# Interactive analytics dashboard (tablet / iPad optimized)
# ---------------------------------------------------------------------------
streamlit==1.36.0

# ---------------------------------------------------------------------------
# Visualization — interactive charts and strike-zone heat maps
# (plotly 5.22.0 already in requirements_phase_3.txt; re-declared for clarity)
# ---------------------------------------------------------------------------
plotly==5.22.0
kaleido==0.2.1            # Plotly static PNG export (used when embedding charts in PDF)

# ---------------------------------------------------------------------------
# PDF report generation
# ---------------------------------------------------------------------------
reportlab==4.2.0
Pillow==10.4.0            # image handling inside ReportLab (logos, player photos)

# ---------------------------------------------------------------------------
# Slack notifications
# ---------------------------------------------------------------------------
slack-sdk==3.30.0

# ---------------------------------------------------------------------------
# Jinja2 — HTML email template rendering
# ---------------------------------------------------------------------------
jinja2==3.1.4

# ---------------------------------------------------------------------------
# HTTP client — async API calls from dashboard to FastAPI backend
# (httpx 0.27.0 already in requirements.txt; re-declared for clarity)
# ---------------------------------------------------------------------------
httpx==0.27.0

# ---------------------------------------------------------------------------
# AWS SES — transactional email dispatch
# (boto3 1.34.84 already in requirements.txt)
# ---------------------------------------------------------------------------
boto3==1.34.84
